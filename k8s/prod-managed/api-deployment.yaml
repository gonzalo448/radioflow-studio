apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radioflow-media-pvc
  namespace: radioflow
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
---
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: radioflow
spec:
  selector:
    app: radioflow-api
  ports:
    - name: http
      port: 4000
      targetPort: 4000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: radioflow-api
  namespace: radioflow
spec:
  replicas: 2
  selector:
    matchLabels:
      app: radioflow-api
  template:
    metadata:
      labels:
        app: radioflow-api
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: api
          image: radioflow-studio-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4000
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "4000"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: radioflow-secrets
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: radioflow-secrets
                  key: REDIS_URL
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: radioflow-secrets
                  key: JWT_SECRET
            - name: CORS_ORIGIN
              value: "none"
            - name: CORS_CREDENTIALS
              value: "true"
            - name: BODY_LIMIT_BYTES
              value: "1048576"
            - name: MEDIA_ROOT
              value: /app/media
            - name: REFRESH_TOKEN_CLEANUP_INTERVAL_MIN
              value: "30"
            - name: REFRESH_TOKEN_REVOKED_RETENTION_DAYS
              value: "14"
            - name: REFRESH_TOKEN_EXPIRED_RETENTION_DAYS
              value: "3"
            - name: REFRESH_TOKEN_CLEANUP_MAX_DELETE
              value: "2000"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 768Mi
          volumeMounts:
            - name: media
              mountPath: /app/media
          readinessProbe:
            httpGet:
              path: /api/health/ready
              port: 4000
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /api/health
              port: 4000
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 5
      volumes:
        - name: media
          persistentVolumeClaim:
            claimName: radioflow-media-pvc
